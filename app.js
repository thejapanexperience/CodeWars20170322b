var recoverSecret = function(triplets) {
  console.log('triplets: ', triplets)
  let secret = []
  secret.push(triplets[i][0]);
  console.log('secret: ', secret)
  for (var i = 0; i < triplets.length; i++) {
  }
}

let triplets = [
  ['t', 'u', 'p'],
  ['w', 'h', 'i'],
  ['t', 's', 'u'],
  ['a', 't', 's'],
  ['h', 'a', 'p'],
  ['t', 'i', 's'],
  ['w', 'h', 's']
];

recoverSecret(triplets)
